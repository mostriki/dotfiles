# ---------------------------------
# Common aliases
# ---------------------------------



# ---------------------------------
# Git aliases
# ---------------------------------

gp () {
    git push origin `git branch -l | awk '/*/{print $2}'`
}

# ---------------------------------
# Kubernetes aliases
# ---------------------------------
