# ---------------------------------
# Common aliases
# ---------------------------------



# ---------------------------------
# Git aliases
# ---------------------------------

alias gp=git push origin `git branch -l | awk '/*/{print $2}'`

# ---------------------------------
# Kubernetes aliases
# ---------------------------------
